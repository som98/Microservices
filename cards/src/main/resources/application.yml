server:
  port: 9000

spring:
  application:
    name: cards
  datasource:
    url: jdbc:h2:mem:testdb3
    driverClassName: org.h2.Driver
    username: root3
    password: pass3
  h2:
    console:
      enabled: true
      path: /h2-db3
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application-qa.yml"
      - "application-prod.yml"
  profiles:
    active:
      - "qa"

build:
  version: "1.0"

cards:
  message: "Welcome to SomshubhraRoy Cards related local APIs "
  contactDetails:
    name: "Somshubhra Roy - Developer - LOCAL"
    email: "somshubhraroy98@gmail.com"
  onCallSupport:
    - (555) 555-1234
    - (555) 523-1345